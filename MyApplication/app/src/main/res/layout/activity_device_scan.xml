<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.eecs149.block.DeviceScanActivity">

    <ImageView
        android:id="@+id/ivBlockScanLogo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:adjustViewBounds="true"
        android:padding="8dp"
        android:scaleType="centerInside"
        android:src="@drawable/block_title" />

    <TextView
        android:id="@+id/tvConnectInstructions"
        android:paddingTop="8dp"
        android:paddingBottom="5dp"
        android:textSize="20dp"
        android:gravity="center_horizontal"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/ivBlockScanLogo"
        android:text="@string/select_device" />

    <View
        android:id="@+id/vBreak"
        android:layout_width="fill_parent"
        android:layout_height="1dp"
        android:paddingBottom="5dp"
        android:layout_below="@id/tvConnectInstructions"
        android:background="@android:color/darker_gray" />

    <ListView
        android:id="@+id/lvDevicesFound"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/vBreak" />

</RelativeLayout>
